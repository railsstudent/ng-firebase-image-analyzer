import { DownloadEnhancedDirective } from './download-enhanced';

describe('DownloadEnhanced', () => {
  it('should create an instance', () => {
    const directive = new DownloadEnhancedDirective();
    expect(directive).toBeTruthy();
  });
});
